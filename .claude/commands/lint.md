Run ESLint on the project and fix any auto-fixable issues.

1. Run `yarn lint` and capture the output
2. If there are errors that can be auto-fixed, run `yarn lint:fix`
3. Report which files had issues and what was fixed vs what needs manual attention
